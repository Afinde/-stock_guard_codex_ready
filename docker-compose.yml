services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
  scheduler:
    build: .
    command: ["python", "-m", "app.scheduler"]
    env_file:
      - .env
    volumes:
      - ./data:/app/data
